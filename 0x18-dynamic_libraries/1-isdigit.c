/**
 * _isdigit - Checks if a character is a number
 * @c: variabe to be be checked
 * Return: 1 if it's a number and 0 if else
 */
int _isdigit(int c)
{
if (c >= 48 && c <= 57)
{
return (1);
}
else
{
return (0);
}
}
